var err_field = document.getElementById('errField');
err_field.innerHTML = "Du er nu tilmeldt vores nyhedsbrev. Vi glæder os til at vise dig nyt.";
var input_fields = document.querySelectorAll('input');

\// Remove input value
Array.from(input_fields).forEach(function(input) {
input.value = "";
});
