- if @cart_items.any? 
  %h2.text-center Din kurv
  %ul.product-detail-list
    = render partial: 'cart_items/item', collection: @cart_items
  %h6.text-right Samlet pris: #{pretty_amount_helper(@cart.get_full_price)}
  = link_to "Gå til Checkout &#8594;".html_safe, checkout_path(), class: "btn btn-primary go-to-checkout-btn float-right" if @cart_items.any?

- else
  .empty
    .empty-icon
      %i.icon.icon-download
    %p.empty-title.h5 Du har ikke lagt noget i din kurv endnu
    %p.empty-subtitle Mangler du inspiration?
    .empty-action
      = link_to "Se alle titler", all_titles_path(), class: "btn btn-primary"

